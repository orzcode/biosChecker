<div class="submissionDetails">
	<p>You will get notified when a new BIOS update is available for:</p>
	<p><strong><%= selectedMobo %></strong>.</p>

	<% const trimmedImgLink = selectedMobo.replace(/\//g, ""); %>
	<img
	loading="eager"
	  class="moboThumb"
	  src="https://www.asrock.com/mb/photo/<%= trimmedImgLink %>.png"
	  alt="Selected Motherboard"
	  width="300"
	/>

	<% if (typeof previousMobo !== 'undefined' && previousMobo) { %>
	<em>
	  (Previously you were signed up for:
	  <strong><%= previousMobo %></strong>).
	</em>
	<% } %>

	<p><strong>Please note:</strong> checks are performed daily, but during large updates, your notification may be delayed by 24hr due to tier limits.</p>

	<div class="buttonsDiv">
	  <button onclick="window.location.href='/'">Return to Main</button>
	  <p>or</p>
	  <form action="/unsubscribe" method="POST">
		<input type="hidden" name="email" value="<%= email %>" />
		<button type="submit" class="unsub">Unsubscribe</button>
	  </form>
	</div>
  </div>